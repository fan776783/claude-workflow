# {{spec_name}}

> {{one_line_description}}

<!--
写作原则：
- Document Reality, Not Ideals — 记录项目真实做法，而不是理想规范
- 每条规则配真实代码示例（从本仓库挑，不虚构）
- 每条规则说明 Why（一句话即可）
- 至少 1 个 anti-pattern / Common Mistake

必备段：Overview / Rules / DO / DON'T / Common Mistakes
可选扩展：Patterns / Examples / Quick Reference / Reference Tables / Strategy / Checklist

小节命名：
- Rules 段下的每个小节**用具体语义名**作为 H3（如 `### Top-Level Catch Pattern`、`### 使用 <script setup>`），
  不要保留 `### Rule: ...` 这种模板前缀
- Common Mistakes 段下同理，用 `### Missing Why Comment` 这样的具体名，而不是 `### Mistake: ...`
-->

---

## Overview

<!-- 必备。1–2 段，说明本规范覆盖什么、为什么存在。 -->

(To be filled) — 本规范覆盖 {{scope}}，存在的原因是 {{reason}}。

---

## Rules

<!--
必备。至少 1 条规则；每条含：
1. 具体语义名作为 H3 标题（不要保留 "Rule:" 前缀）
2. 规则正文（1–2 句）
3. 真实代码示例
4. **Why** 一句话
-->

### (替换为规则的具体语义名)

(To be filled) — 规则正文。

```{{lang}}
// 真实代码示例（从本仓库挑，不虚构）
```

**Why**: (To be filled) — 一句话解释原因。

---

## DO / DON'T

**DO**

- (To be filled)
- (To be filled)

**DON'T**

- (To be filled)
- (To be filled)

---

## Common Mistakes

<!--
必备。至少 1 个 anti-pattern。每个小节：
1. 具体语义名作为 H3 标题（不要保留 "Mistake:" 前缀）
2. Bad / Good 对比代码
3. Why it matters 一句话
-->

### (替换为反例的具体语义名)

**Bad**

```{{lang}}
// 反例
```

**Good**

```{{lang}}
// 正例
```

**Why it matters**: (To be filled) — 为什么这个错误值得单独拎出来。

---

<!--
以下为可选扩展块（按需添加，不必全写）。大段之间保持 --- 分隔线。

- Patterns：独立的"模式集合"，每个 Pattern 一个具体命名的 H3 小节 + 代码
- Examples：完整可运行示例
- Quick Reference：速查表、命令清单、缩写映射
- Reference Tables：Exit Codes / HTTP Status / 字段字典 等
- Strategy：架构级策略说明（比 Overview 更深）
- Checklist / Quality Checklist：可逐项勾选的质量检查清单
-->
