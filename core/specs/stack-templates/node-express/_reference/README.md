# Node + Express 参考范例（TODO）

> 本目录放"已经填满的"主题范例，供用户照着 mimic。
>
> 当前缺的范例：
> - `directory-structure.md`
> - `error-handling.md`
>
> 编写原则：所有占位符都替换成具体值，用一个统一的简单业务场景（如 todo API）串起来。
>
> `_reference/` 不会被 `/spec-bootstrap` 拷贝，也不会进入用户项目。
