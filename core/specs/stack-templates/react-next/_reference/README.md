# React + Next 参考范例（TODO）

> 本目录放"已经填满的"主题范例，供用户照着 mimic。
>
> 当前缺的范例：
> - `component-guidelines.md`（用 todo-list / blog 场景）
> - `directory-structure.md`
>
> 编写原则：所有占位符都替换成具体值，用一个统一的简单业务场景串起来，让用户能看到"填完应该长什么样"。
>
> `_reference/` 不会被 `/spec-bootstrap` 拷贝，也不会进入用户项目；仅作为模板作者之间的参考文件保留在仓库里。
